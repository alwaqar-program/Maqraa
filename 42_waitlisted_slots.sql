-- ============================================================
-- 42_waitlisted_slots.sql — تمييز اختيارات قائمة الانتظار
-- عند إرسال الاستمارة يُحفظ أي المواعيد كانت مكتملة لمسار الطالبة
-- لحظة اختيارها (قائمة انتظار)، لتظهر للإدارة في صفحة المتقدمات.
-- الاستمارات القديمة تبقى فارغة (المعلومة لم تكن تُسجل). آمن لإعادة التنفيذ.
-- ============================================================

ALTER TABLE public.applicants
  ADD COLUMN IF NOT EXISTS waitlisted_slots text[] NOT NULL DEFAULT '{}';

SELECT 'waitlisted slots ready' AS status;

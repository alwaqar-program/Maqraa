-- ============================================================
-- 40_teacher_color.sql — لون لكل مسمعة
-- يميّز حلقاتها في تقويم الفرز وجداول الحلقات، ويُحرَّر من صفحة المسمعات.
-- فارغ = يأخذ لونًا ثابتًا من لوحة النظام حسب ترتيبها.
-- آمن لإعادة التنفيذ.
-- ============================================================

ALTER TABLE public.teachers ADD COLUMN IF NOT EXISTS color text
  CHECK (color IS NULL OR color ~ '^#[0-9A-Fa-f]{6}$');

SELECT 'teacher color ready' AS status;
